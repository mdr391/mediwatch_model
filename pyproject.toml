[project]
name = "mediwatch-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.1"
dependencies = [
    "apache-airflow>=3.1.7",
    "evidently>=0.7.20",
]
